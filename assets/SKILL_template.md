---
name: REPLACE-with-kebab-case-name
description: REPLACE — state WHAT the skill does and WHEN to use it (situations, user phrasings, file types). Lean slightly pushy on trigger conditions, but never claim capabilities the skill lacks. Example "Trigger on '…', '…'."
---

# REPLACE — Skill Title

One sentence: what this skill does.

## When to use this skill

- Bullet the concrete situations that should invoke it.

## Guiding principles

- Any constraints or non-negotiables. (Keep behavior predictable — least surprise.)

## Workflow

1. Step one.
2. Step two.
3. Step three.

## Examples

Input:
```
REPLACE with a representative input
```
Output:
```
REPLACE with the expected output
```

## Edge cases

- Unusual input → how to handle it.

## Bundled resources

- `references/REPLACE.md` — read when REPLACE.
- `scripts/REPLACE.py` — run to REPLACE.
- `assets/REPLACE` — used in output for REPLACE.

<!--
Keep this body under ~500 lines. If it grows past that, move detail into references/ files
and leave one-line pointers here ("For X, read references/x.md").
-->
